# Ported to TensorFlow 2.15 / Python 3.11 for Hopper (sm_90) GPUs.
# TF 2.15 bundles keras 2.15 (no standalone keras / keras-nightly / tensorflow-estimator).
# The CUDA 12 runtime libraries come from `tensorflow[and-cuda]` installed in the image;
# pinning plain `tensorflow` here just asserts the version (pip sees it satisfied).
# opencv (cv2) is provided by the container image, so it is intentionally not pinned here.
tensorflow==2.15.1
numpy==1.26.4
